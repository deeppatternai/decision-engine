# Decision Engine 发版流程

本文定义从开发仓（A）到发布仓（B）的正式发版边界。发布工具、签名私钥和本机路径不属于
仓库内容，也不得提交或分发。

## 仓库职责

- **A 仓（开发仓）**：代码修改的唯一入口。功能、缺陷、版本号和测试均在 A 仓修改，
  完成提交并推送到远程 `main`。
- **B 仓（发布仓）**：只从远程同步已经推送的 `main`，执行发布验证和正式发布。禁止在
  B 仓编辑代码、创建修复提交、cherry-pick 修复或合并开发分支。
- A 到 B 只通过远程 `main` 传递，不复制本地工作区文件。B 必须保持干净，并且发布时
  `HEAD` 必须与 `origin/main` 指向同一个提交。
- B 发现问题后只记录和输出证据；需要修改时返回 A 仓处理。A 完成修复、测试、提交和
  推送后，B 再同步最新 `main` 并重新验证。

## 发布验证

验证范围以“上一版已签名源码提交”到“本次候选 `origin/main` 提交”的差异为准：

1. 优先采用目标提交上已经通过并覆盖本次变更的可信 CI 结果。
2. CI 没有覆盖时，只运行与变更文件和行为直接相关的测试、语法检查及静态检查。
3. 默认不运行本地完整测试套件。只有变更跨越多个核心模块、无法可靠确定影响范围、目标
   提交的可信 CI 不完整，或者发布管理员明确要求时，才升级为完整测试。
4. CI 证据必须对应本次准备发布的精确提交；其他提交上的成功结果不能替代本次验证。

## 问题分级

以下问题阻断发布，并返回 A 仓修改：

- 签名、密钥、信任库、manifest、tag、release sequence 或 `stable` 状态异常；
- 安装、迁移、自动更新或发布完整性可能失败；
- 权限、凭据泄露、数据损坏或不可恢复风险；
- 核心流程崩溃、来源提交不一致；
- 本次变更相关的必需测试或 CI 失败。

不影响安装、更新、签名信任及本次核心功能的低风险问题不阻断当前发布，例如文案建议、
非发布路径中的既有小问题，或不影响正确性的测试改进建议。此类问题必须记录，但不得在
B 仓临时修改。无法确定严重级别时按阻断处理，由发布管理员判断。

## B 仓问题输出

B 仓发现问题时输出以下信息，供 A 仓修改：

- 严重级别：阻断或不阻断；
- 影响范围和可复现现象；
- 对应文件、提交、CI 或命令证据；
- 建议在 A 仓执行的修复方向和验证命令；
- 当前发布是继续还是停止。
